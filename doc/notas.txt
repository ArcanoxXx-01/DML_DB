training id , dataset id, model id 1, status 1, model id 2, status 2, ...  hasta 10 // Tabala de trainings



Endpoints:

1- Que modelos ejecutar: para que back-ends ejecuten modeos:

    get : /api/v1/models/torun

    Devolver un unico id de un modelo y la tarea y el dataset_id 

2- Crear un training

    Post :

    training_id,
    dataset_id,
    training_type,
    models: List[models_names]

3-  Get training

    training_id:

    Oitput un dict: 
    {
        training_id,
        dataset_id,
        training_type,
        models_ids: LIst,
    }

4- Guardar modelos:

    Input:
    {
        model_id,
        model_data: dict
    }

5- Get model:

    /api/v1/models/{model_id}

    Ontput: 
    {
        model_data:dict
    }

6- Save model runinig status:

    /api/v1/models/healt/{model_id}

    // actualizar la ultima vez

7- Save training Results:

    Input:
    {
        model_id,
        results:dict
    }
    results = {
        metric_1,
        metric_2,
        ...
    }
    // Crear una tabla con model_id, metric_1, metric_2, ... // hasta el tope de las metricas que se defina

8- Save Prediction:

    {
        model_id,
        dataset_id,
        prediction_list: json
    }

9- Get Dataset:

    /api/v1/datasets/{dataset_id}/{batch}

    // los batch son de tamaño 64

